static const float3 colourTL = float3(1.0, 0.0, 0.0);
static const float3 colourTR = float3(1.0, 1.0, 0.0);
static const float3 colourBL = float3(1.0, 0.0, 1.0);
static const float3 colourBR = float3(1.0, 1.0, 1.0);
